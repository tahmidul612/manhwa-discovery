# Docker Compose configuration
# TODO: Configure services

version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=
    # volumes:
    #   - ./backend:/app
    
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    # volumes:
    #   - ./frontend:/app
    
  # database:
  #   image: postgres:15
  #   environment:
  #     POSTGRES_DB: manhwa_discovery
  #     POSTGRES_USER: user
  #     POSTGRES_PASSWORD: password
  #   volumes:
  #     - db_data:/var/lib/postgresql/data
  
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"

# volumes:
#   db_data:
